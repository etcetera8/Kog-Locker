export const addUserData = (userData) => ({
  type: 'ADD_USER_DATA',
  payload: userData
})

// add user stats
export const addUserStats = (userStats) => ({
  type: 'ADD_USER_STATS',
  payload: userStats
})

// add user target segment
// 

//get user activities